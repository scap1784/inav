#ifndef NODE_HPP
#define NODE_HPP
#include "shape.hpp"

template< typename T >
class Node {
  protected:
    T id_;
    uint32_t color_;
    Shape shape_;

  public:
    Node();
    void  setNodeID( T id );
    T getNodeID( );
    void setNodeColor( uint32_t color );
    uint32_t getNodeColor( );
    void setNodeShape( Shape shape );
    Shape getNodeShape( );
};

#endif
